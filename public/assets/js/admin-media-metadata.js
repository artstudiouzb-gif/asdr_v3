(function () {
    'use strict';

    // Legacy compatibility shim.
    //
    // Раньше этот файл создавал отдельный gallery-media-modal и в capture-фазе
    // перехватывал [data-media-gallery-pick], из-за чего фотогалерея новости
    // использовала второй, независимый от основной медиабиблиотеки picker.
    // Основной admin.js уже предоставляет единый API:
    //     window.MediaPicker.pickMany(callback)
    // и сам обрабатывает [data-media-gallery-pick].
    //
    // Оставляем файл как пустой shim, чтобы старые шаблоны/кэши, которые ещё
    // запрашивают этот URL, не ломали страницу. Новых обработчиков, модалок,
    // сеток и логики выбора здесь быть не должно.
})();
