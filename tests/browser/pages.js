/**
 * Список публичных страниц, который обходят браузерные проверки.
 *
 * Он один на все спеки намеренно: доступность и горизонтальное переполнение
 * ловятся на одних и тех же страницах, и два списка разъехались бы при первом
 * же добавлении — новая страница попадала бы под одну проверку и оставалась
 * без другой.
 *
 * Адреса берутся из демо-контента, который поднимает CI (seed_visual.php).
 */
const PUBLIC_PAGES = [
    ['главная', '/'],
    ['лента новостей', '/news'],
    ['поиск', '/search?q=%D1%81%D1%82%D1%80%D0%B0%D1%82%D0%B5%D0%B3%D0%B8%D1%8F'],
    ['узбекская версия', '/uz'],
    // Формы и таблицы — там нарушений больше всего: у полей теряются подписи,
    // у ячеек — заголовки. Детальная новость и проект добавлены как самые
    // читаемые страницы сайта.
    ['детальная новость', '/news/ekspertnyy-kadrovyy-rezerv'],
    ['проект', '/projects/cifrovaya-transformaciya'],
    ['каталог документов', '/catalog/documenty'],
    ['контакты с формой', '/kontakty'],
    ['страница со слайдером', '/antikorrupciya'],
];

module.exports = { PUBLIC_PAGES };
